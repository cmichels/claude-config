# Context Dump: claude-config
## Generated: 2026-05-05T18:25:02Z
## Plan: None (ad-hoc allowlist refinement session)

## Status at Suspend
Clean stop. Branch is up to date with `origin/dev` at `e9edc6c`. Six commits shipped today. Two untracked files (`.context-dump.md`, `.task-context`) still need a `.gitignore` decision — proposed twice this session, kuda hasn't executed either acknowledgment.

## Completed This Session
- **Resumed from 2026-05-01 suspend** — picked up the pending `Bash(actionlint*)` addition and the open `golangci-lint run*` → `golangci-lint*` broadening question.
- **Shipped 6 commits to `dev` (all GPG-signed, all pushed):**
  - `d1c557b` Allow `Bash(actionlint*)` (carried over from prior suspend)
  - `b185c3b` Allow `Bash(gh search*)` (broadened from `gh search code*`)
  - `ccbfe1d` Allow `Read(../optelligent/**)` (relative path)
  - `3a278c5` Allow `Bash(golangci-lint version*)` (narrow — immediately superseded)
  - `94337ea` Allow `Bash(golangci-lint*)` (consolidation, supersedes `run*` and `version*`)
  - `0109b69` Allow `Read(/home/kuda/projects/optelligent/**)` (absolute path)
  - `e9edc6c` Allow `Bash(yamllint*)` + recovered 4 entries from external file stomp
- **Resolved the `golangci-lint` broadening loop** that was open across multiple suspends. Final answer: broad consolidated entry.
- **Recovered from external `settings.json` stomp** at ~09:16:35 EDT — patched forward via Edit tool (not `git checkout`) per kuda's instruction. Stomp cause unknown.
- **Discussed and declined adding `Bash(tee*)`** to global allow — general-purpose write primitives don't fit the "reads/builds/queries free, writes path-scoped" posture. Reopen criteria: 5+ tee prompts/week.

## Remaining / Open Loops

### Immediate (this session, unaddressed)
- **`.task-context` and `.context-dump.md` need `.gitignore` entries.** Diff proposed twice:
  ```diff
   # Tasks & todos (session-specific)
   tasks/
   todos/
  +.task-context
  +.context-dump.md
  ```
  Kuda said "commit and push" twice without other changes pending — most likely interpretation was acknowledgment of this proposal, but never explicitly confirmed. Pick up here on resume.

### Carryover (from prior suspends, still open)
- **`Bash(go run)` and `Bash(go install)`** — last Go toolchain members not allowlisted. Add on first prompt.
- **Redundant `Bash(go mod tidy*)`** — subsumed by `Bash(go mod *)`. Same consolidation pattern as today's `golangci-lint*`. Pruneable.
- **`cdraft()` shell helper** (`claude -p "..." | win32yank -i`) — sketched, never built.
- **Vibe-check rule in CLAUDE.md** — live but unproven. Escalate to anti-example memory if deadness creeps back.

### NEW open loop — investigate file rewrite stomp
- **`settings.json` was externally rewritten at 2026-05-05 09:16:35 EDT** mid-session. Kuda confirmed it was not them and not other Claude sessions. The rewrite reverted 4 committed allowlist entries (the two `Read(...optelligent...)`, the `gh search*` broadening, the `golangci-lint*` consolidation) and reordered top-level JSON keys (moved `model`, `teammateMode`, `useAutoModeDuringPlan`, `remoteControlAtStartup` to bottom). Suspected culprits: Claude Code's `/config` UI, a file watcher, or a daemon round-tripping JSON. **If reproduces, capture timing data to correlate with UI actions.**
- **Recovery method that worked:** patch-forward via Edit tool. Avoid `git checkout` — kuda was concerned a checkout would re-trigger the stomper.

## Key Decisions This Session
- **Three-commits-per-tweak.** Today's 6 commits each isolate one change, matching `1a6deba`-style precedent. Even superseded commits (`3a278c5`) stay in history because amending is destructive — bisectable history > tidy log.
- **Broad over narrow for sibling-safe subcommands.** `gh search*` and `golangci-lint*` were both consolidated from narrow patterns once their subcommand surfaces were verified safe (read-only, no destructive variants). Same logic that's pending for `go mod *`.
- **Absolute-and-relative pair for `optelligent` reads.** Mirrors the existing Write pair for the same dir. Belt-and-suspenders covers all CWDs.
- **Patch-forward recovery, not checkout-and-restore.** Kuda's call. Reduces risk of provoking the unknown rewrite agent. Worked cleanly.
- **Rejected `Bash(tee*)`.** General-purpose write primitives don't fit the allowlist posture. Path-scoped (`Bash(tee /tmp/*)`) is the fallback if friction shows up.

## Modified Files
- `settings.json` — clean wrt tracked files (committed and pushed at `e9edc6c`)

## Untracked
- `.context-dump.md` (this file, regenerating)
- `.task-context` (task-ctl metadata)

## Blockers
- None.
